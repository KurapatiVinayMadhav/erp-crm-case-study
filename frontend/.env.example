# Frontend environment variables. Copy to `.env` and adjust if needed.

# Optional: point the built app at a remote API (used on Vercel/Netlify).
# In local development the Vite dev server proxies /api to the backend,
# so this can stay unset.
VITE_API_URL=""