server {
    listen 80;
    server_name _;

    root /usr/share/nginx/html;
    index index.html;

    # Optional: proxy the API when the backend runs behind the same host.
    # Override with an env-substituted value in production if the API lives
    # on a separate domain (see the deploy action / README).
    location /api/ {
        proxy_pass http://backend:4000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    }

    location /health {
        proxy_pass http://backend:4000;
    }

    location / {
        try_files $uri /index.html;
    }
}